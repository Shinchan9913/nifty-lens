[pytest]
# Run from the repo root; make `src` importable as a package in tests.
pythonpath = .
# Let later phases write `async def test_...` without per-test decorators.
asyncio_mode = auto
testpaths = tests
